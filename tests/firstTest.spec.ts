describe("First test", () => {
  describe("when x = 1 and y = 2", () => {
    it("returns3", () => {
      const x = 1;
      const y = 2;

      const sum = x + y;

      expect(sum).toBe(3);
    });
  });
});
